# LabourBook Backend — Python Dependencies
# Install with: pip install -r requirements.txt

# --- Web Framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6

# --- Database (PostgreSQL async) ---
# psycopg[binary] ships pre-built wheels; no compiler required.
# SQLAlchemy uses: postgresql+psycopg://...
sqlalchemy[asyncio]==2.0.36
psycopg[binary]==3.3.4
alembic==1.13.3

# --- Config & Env ---
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- HTTP (internal use / testing) ---
httpx==0.27.2

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0
